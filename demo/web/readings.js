// The prose following each live explorer is deliberately specific to its data.
// It is a reading path, not a second set of controls.
export const READINGS = {
  git4data: [
    ['先看共同起点','创建快照时，数据库记录了一个可再次引用的状态。候选库从该状态 Clone：图中的 A–D 表示两套数据库引用的既有数据块，Δ 表示之后在候选库写入的变化。这里的 A–D 只是帮助理解引用关系，不对应四个真实对象。'],
    ['再看两套数字','切到 Clone，核对候选库的 1,000,000 行与创建时的正式库基线。正式库继续接收订单后，当前行数会超过这个基线，候选库仍保留自己的状态。图中行数来自此刻的 COUNT(*)；CI 价格来自当时执行的验证，二者时间点不同。'],
    ['最后看 CI 的价值','候选库里故意写入负价，质量门禁会失败；修复后通过，再读取正式库的价格以证明隔离。Clone 耗时是本次本机记录。共享已有数据块说明不需要逐行复制百万条订单，但元数据和后续写入仍占空间；这里没有把行数换算成节省的存储字节。'],
  ],
  vector: [
    ['向量如何变成索引','六件商品各有一组四维数值。IVF 在建索引时训练中心，并把每件商品放入一个中心对应的 entries 列表。图中的菱形和点位来自索引内部表；二维投影只帮助看分布，SQL 排序始终计算原始四维距离。'],
    ['拖动查询，读三层结果','切换“旅行充电 / 游戏设备 / 无线音频”会改变查询向量。先看它离哪个中心近，再看每个中心实际有哪些商品，最后看右侧 SQL Top 3 和六行全量距离 Top 3 的重合数。这三层分别对应索引结构、候选范围与最终返回，不能互相替代。'],
    ['改变 nprobe，观察范围','nprobe 控制初始探测的中心数。扩大它通常会增加扫描工作量，也可能改变召回；本机 4.2.4 的搜索还可能继续扩展，所以最终结果可以包含初始中心以外的商品。对六件样本应看语法、计划和数据流；下面的 1200 向量健康实验才适合观察中心负载与重建。'],
    ['验证是否真的走索引','建索引后仍需用 EXPLAIN 查找 ivf_search。实际业务还要在自己的数据上比较查询延迟、召回、空中心和负载比，并在数据分布变化后复查。本页给出每个中心的真实计数与可执行重建，不把这六件商品的图当性能结论。'],
  ],
  fulltext: [
    ['一条文本怎样进入倒排表','选中商品文本后，左栏是实际名称和描述；中栏是当前词项的 doc_id 与位置 pos。倒排表让数据库从词项找到文档，再由业务 ID 返回商品。切到 JSON 或 DOCX 时，索引入口分别变成 JSON parser 与 Stage 中 DATALINK 指向的文件正文。'],
    ['为什么排序会改变','默认 travel 词项在两份商品文本中各出现一次。切换 TF-IDF 和 BM25，比较右栏分数及下方文档长度：本机样本的 TF-IDF 两条同分，而 BM25 把较短文档排在前面。分数来自 MATCH 查询，不是图形脚本模拟的条长。换成 charger 或 battery，可看到另一组倒排记录。'],
    ['把命中与执行计划分开核对','词项出现在倒排表，说明索引收录了它；MATCH 返回行，说明查询确实命中；EXPLAIN 出现 fulltext_index_scan，才说明这条 SQL 的执行路径使用了全文索引。布尔查询、JSON 检索和文档检索还可在后面的 SQL 控制台逐项运行。'],
  ],
  ingest: [
    ['先弄清两个名字','demo_supplier 是 Stage：它把 file:// 目录变成可引用的 stage:// 地址。supplier_feed 是外表：它规定 CSV 的七列、类型和格式。CREATE STAGE 和 CREATE EXTERNAL TABLE 都没有把商品行写进 demo_shop.products；先用 SHOW STAGES 和 SELECT 外表确认路径、映射和原始七行。'],
    ['哪一步真的写了东西','质量表里的 #7 不是外表自动“拦截”的结果；前一章节明确执行了 INSERT ... SELECT，把价格或库存不合格的行写入 quality_issues。表格中“本次 SELECT”来自只读规则试算，可以切换门槛；已写入的质量表和商品表不会跟着变化。'],
    ['demo_load_catalog 从哪里来','下一章节的 CREATE TASK 为库内作业取名 demo_load_catalog，BEGIN ... END 中只有一个 INSERT ... SELECT：取外表中 price>0、stock>=0 且目标表不存在相同 product_id 的商品。EXECUTE TASK 按这个名字手动触发；SHOW TASK RUNS 或 mo_task.sql_task_run 才能确认 SUCCESS 与影响行数。'],
    ['Task 之外还有两类准备','入库后业务商品表有六行。随后脚本分别补入六组预置向量与一条指向 Stage DOCX 的 DATALINK，再用 generate_series 生成一百万行订单。这些动作在 Task 之外执行；向量、文档检索和大表 Clone 分别使用这些数据。'],
  ],
  htap: [
    ['从事务开始','下单脚本在一次事务里扣商品库存、插入订单，提交后读取刚写入的订单号与金额。图的左半边显示当前表中的那笔记录和库存，方便先核对写入是否完整。'],
    ['再读同一份数据','右半边的 COUNT(*) 与 SUM(amount) 直接来自 demo_shop.orders 聚合查询。再次下单后重新打开此页会看到最新值；这展示交易变化如何进入分析查询。这里没有测量吞吐、并发或报表刷新延迟。'],
  ],
  recovery: [
    ['PITR 的保护从何时开始','策略需要在事故前生效。创建策略之前或滚动窗口之外的时间不属于本次可恢复范围；本例先开启一小时窗口，再写入商品并记录服务器时间。'],
    ['选对时间点比命令成功更关键','下方 PITR 实验保存了正常价格和全零价格两个时间点。两个时刻都是六行；恢复后逐列比较名称和价格，才能判断是否找回业务所需状态。'],
    ['从表不存在到逐列相等','DROP TABLE 后保存数据库原始错误。恢复之后重新 SELECT 名称和价格，并与所选历史点逐列比较。时间线保存的是实测记录，右侧展示最近一次恢复或查询的结果。'],
  ],
  pubsub: [
    ['发布的是确定范围','源账号将 demo_shop.products 发布为 Publication。伙伴账号的 Subscription 映射为 partner_catalog.products；这个演示只开放商品目录，其他业务表不在发布范围。'],
    ['切换账号看结果','上方切换会展示两个账号各自连接查询得到的行。源账号和订阅账号读取的是同一个目录的发布视图，但数据库名称与身份不同；比较行内容比只看对象存在更有说服力。'],
  ],
  iceberg: [
    ['先确认湖表真实存在','第 2 步的 REST CreateTable 返回 schema 和 metadata-location。只创建 MatrixOne Catalog 名称不会自动建立湖表；只有文件目录也还不足以构成一张 Iceberg 表。'],
    ['映射不是数据复制','第 3 步把本地表名连接到远端 namespace/table/ref。实际订单由第 4 步 INSERT 产生：SQL 写入湖上的文件并提交快照，再从同一外表读回来。'],
    ['文件清单决定读哪个状态','metadata JSON、manifest list 和 manifest 描述快照中的数据文件。页面列出的目录文件是存储证据，可能含保留的旧对象，不等于查询扫描清单。'],
    ['历史表名是我们创建的','append_orders_old 既不是内置表，也不是自动生成的历史副本。它映射到同一湖表的 Nessie 历史分支。Catalog 分支名和 Iceberg snapshot ID 分开显示。'],
    ['差异落到具体订单','main 追加第五笔订单之后，历史分支仍有前四笔。按订单 ID、金额与真实快照 ID 核对，才能说明两个状态为什么不同。'],
  ],
  cdc: [
    ['从一个可追踪的订单出发','CDC 任务把 MatrixOne 订单变化送到 MySQL。演示生成新订单号后，固定这个 ID 分别查询源端与目标端；两边不能用各自最新的一行代替核对。'],
    ['以行内容判断到达','图中逐列列出 product_id、数量与金额。任务创建成功和检查点推进只能说明链路状态；同一订单在目标端出现且字段一致，才是这次增量交付的直接证据。'],
  ],
};
